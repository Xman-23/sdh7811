  CData문자열
  sql 명령어에
  <기호나
  > 기호가 사용되는 겨우에
  태그와 구별하기 위하여 사용하는 문자열을 말합니다.
  <![CDATA{
  문자열
  ]]>
</mapper>
---------------------------------------------------------------
-	setTimeout 	==> 딱 한번만 지정한 시간이 되면 동작하는 함수
-	setInterval	==> 계속 반복하여 지정한 시간 마다 동작하는 함수

	setTimeout(함수,시간)
	setInterval(함수,시간)
-------------------------------------------------------------
<input ~~~ >
<td></td>
<div><div>
<span></span>

$(선택자).val(값);
$(선택자).html(값), $(선택자).text()